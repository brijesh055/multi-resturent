# NepalBite Environment Variables
# Firebase config is already in src/firebase.js
# No environment variables needed for basic setup

# Optional: set CI=false to prevent warnings from failing the build
CI=false
